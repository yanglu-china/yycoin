dependencies {
    compile project(':oa_product_manager')
}

task compileTask << {
    System.out.println "compiling..."
    println "classpath = ${configurations.compile.collect {File file -> file.name}}"
}